{
  "name": "noida-talent-hunt",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2026-06-13",
    "deploy": "wrangler pages deploy . --project-name=noidatalenthunt"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
